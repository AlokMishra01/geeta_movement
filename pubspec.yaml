name: geeta_movement
description: Flutter app for Hindu Youth Union called Geeta Movement.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3

  google_fonts: ^2.1.0
  infinite_carousel: ^1.0.0
  cached_network_image: ^3.0.0
  pdf_viewer_jk: ^2.1.0
  audioplayers: ^0.19.0

#  adaptive_theme: ^2.2.0
  provider: ^5.0.0
  dio: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/
